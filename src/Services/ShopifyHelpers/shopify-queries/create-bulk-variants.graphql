mutation productVariantsBulkCreate($productId: ID!, $variants: [ProductVariantsBulkInput!]! /MEDIAPARENTINPUT/) {
  productVariantsBulkCreate(productId: $productId, variants: $variants /MEDIAINPUT/) {
    userErrors {
      field
      message
    }
  }
}