mutation productVariantsBulkUpdate($productId: ID!, $variants: [ProductVariantsBulkInput!]! /MEDIAPARENTINPUT/) {
  productVariantsBulkUpdate(allowPartialUpdates: true, productId: $productId, variants: $variants /MEDIAINPUT/) {
    userErrors {
      field
      message
    }
  }
}