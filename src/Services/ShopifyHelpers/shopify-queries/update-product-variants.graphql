mutation call($input: ProductInput!) {
  productUpdate(input: $input) {
    product {
      title
      id
      variants(first: 100) {
        edges {
          node {
            id
          }
        }
      }
    }
    userErrors {
      message
      field
    }
  }
}
