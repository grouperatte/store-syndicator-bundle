mutation call($input: ProductInput!) {
  productUpdate(input: $input) {
    product {
      id
      variants(first: 100) {
        edges {
          node {
            id
          }
        }
      }
    }
    userErrors {
      message
      field
    }
  }
}
