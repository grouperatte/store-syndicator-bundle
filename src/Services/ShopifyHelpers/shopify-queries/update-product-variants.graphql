mutation call($input: ProductInput!) {
  productUpdate(input: $input) {
    product {
      title
      id
      variants(first: 10) {
        edges {
          node {
            id
          }
        }
      }
    }
    userErrors {
      message
      field
    }
  }
}
